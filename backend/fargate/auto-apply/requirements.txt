# Installed into the Playwright image (see Dockerfile).
#
# playwright MUST stay in lockstep with the base image tag in the Dockerfile
# (mcr.microsoft.com/playwright/python:v1.44.0-jammy). The image ships matching
# browser binaries; a different playwright version here means the library and the
# installed browsers disagree and every apply attempt fails on launch.
playwright==1.44.0
playwright-stealth==1.0.6

# Used for SQS/DynamoDB/S3 access from the task. Unlike Lambda, this image has no
# preinstalled boto3, so it is a genuine dependency here.
boto3>=1.34,<2.0
